# Example environment file (.env.example)
# Copy this file to .env and fill in the values for local development.
# WARNING: do NOT commit the real .env file to the repository.

# --- Backend (server) ---
DB_PASSWORD=

# JWT secret used to sign tokens
JWT_SECRET=

# --- Cloudinary / cloud storage ---
CLOUD_NAME=
CLOUD_API_KEY=
CLOUD_API_SECRET=

# --- Frontend (client) ---
# API base URL accessible from the browser. For CRA variables must start with REACT_APP_
REACT_APP_API_URL=
